// Re-export the real Settings component from the TSX file so runtime imports
// that resolve to /src/pages/admin/Settings.js will receive the named export.
export { Settings } from './Settings.tsx';
